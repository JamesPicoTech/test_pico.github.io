# PicoScope 7 documentation

## Prerequisites
- python > 3.10

then `pip install` the following
- mkdocs
- mkdocs-glightbox
- mkdocs-with-pdf

Best practices is to create 

## Development

To develop use the command `mkdocs serve` in the same location as *mkdocs.yml*

## To build 

Use `mkdocs build` to build the site, or use `mkdocs gh-deploy` to deploy the site

